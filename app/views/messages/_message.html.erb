<div id="message-<%= message.id %>">
  <small>
    <strong><%= message.user.first_name %></strong>
    <i><%= message.created_at.strftime("%a %b %e at %l:%M %p") %></i>
  </small>
  <% if uri?(message.content) %>
  <%= link_to "Check this promo!", message.content %>
  <%= message.link %>
  <% else %>
  <p><%= message.content %></p>
  <% end %>
</div>
